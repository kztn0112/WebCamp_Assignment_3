<h2>User info</h2>

<% if book.errors.any? %>
  <%= book.errors.count %>errors prohibited this obj from being saved:
  <% book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

<%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
 <table class="table">
   <tbody>
  <tr></tr>

  <tr>
    <th>name</th>
    <th>
    <h3 class="profile-name"><%= user.name %></h3>
    </th>
  </tr>

  <tr>
  <th>introduction</th>
  <th><h3 class="profile-introduction"><%= user.introduction %></h3>
  </th>
  </tr>

  </tbody>
  </table>


<!--＃書き換え-->
  <p><%= link_to "", edit_user_path(@user.id),class:"fas fa-user-cog" %></p>


<!--#新規投稿-->
<h2>New book</h2>
<%= form_with model:book, local:true do |f| %>
  <div class="form-group">
    <h4>Title</h4>
  <%= f.text_field :title %>
  </div>

  <div class="form-group">
   <h4>Opinion</h4>
  <%= f.text_area :body %>
  </div>

  <%= f.submit 'Create Book' %>
<% end %>